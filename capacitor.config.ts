import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zhariff.quranrevisiontracker',
  appName: 'Quran Revision Tracker',
  webDir: 'dist'
};

export default config;
